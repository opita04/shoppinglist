rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily allow full access for debugging and testing
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Later we'll implement proper security rules like these:
    /*
    match /lists/{listId} {
      allow read, write: if request.auth != null;
    }
    
    match /masterStores/{storeId} {
      allow read, write: if request.auth != null;
    }
    
    match /archivedLists/{listId} {
      allow read, write: if request.auth != null;
    }
    */
  }
} 